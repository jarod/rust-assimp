language: rust
os:
    - linux
before_script:
    - rustc -v
    - cargo -V
install:
  - sudo apt-get update -qq
  - sudo apt-get install libassimp-dev
  - bundle install --path vendor/bundle
script:
    - cargo build -v
    - cargo doc -v
